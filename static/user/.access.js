// 这个文件夹除了

module.exports = (options)=>{
	console.log(commonObj.get("systemConfig"));
	//let query = options.query;// get 参数
	//let filename = options.filename;// 请求的文件名
	//console.log(cache.get("version"));
	return new Promise((resolve,reject)=>{
		//resolve(false);
		resolve(true);
	});
}
